#include "alphabeta.h"
#include "board.h"
#include "move.h"
#include "tools.h"
move getMove(board& state)
{
    //folosim Iterative Deepening.
    int depth = 1;
    while(stillHaveTime())
    {
        ++depth;
        
    }
}